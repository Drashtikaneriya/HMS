
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Welcome to HealthSoft</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;600&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: radial-gradient(circle at top left, #e0f7fa, #ffffff);
            background-image: url('https://www.transparenttextures.com/patterns/cubes.png');
            background-repeat: repeat;
            background-size: auto;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .hero-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 30px;
        }

        .welcome-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(12px);
            border-radius: 20px;
            padding: 50px 40px;
            max-width: 550px;
            width: 100%;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: all 0.3s ease-in-out;
        }

            .welcome-card:hover {
                transform: scale(1.02);
                box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
            }

            .welcome-card img {
                width: 90px;
                margin-bottom: 20px;
            }

            .welcome-card h1 {
                font-weight: 600;
                font-size: 2.3rem;
                color: #007bff;
                margin-bottom: 10px;
            }

            .welcome-card p {
                color: #444;
                font-size: 1rem;
                margin-bottom: 30px;
            }

        .welcome-btn {
            padding: 12px 35px;
            font-size: 1rem;
            font-weight: 500;
            border-radius: 30px;
            background: linear-gradient(to right, #007bff, #00bcd4);
            color: white;
            border: none;
            transition: 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.3);
            text-decoration: none;
        }

            .welcome-btn:hover {
                transform: scale(1.05);
                background: linear-gradient(to right, #0056b3, #008c9e);
                box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
            }

        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-thumb {
            background-color: #007bff;
            border-radius: 5px;
        }

        /* ✅ Media Queries Fixed */
        
    </style>
</head>
<body>
    <div class="hero-container">
        <div class="welcome-card">
            <img src="https://cdn-icons-png.flaticon.com/512/3774/3774299.png" alt="Doctor Icon" width="100">


            <h1>Welcome to <span style="color:#28a745;">HealthSoft</span></h1>
            <p>Smart Hospital Management System<br />Built with .NET to simplify healthcare operations</p>
           
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
